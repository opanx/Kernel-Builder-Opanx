name: Build Kernel GKI - Panxcz Gaming (SUSFS)

on:
  workflow_dispatch:
    inputs:
      variant:
        description: 'Pilih varian KernelSU'
        required: true
        default: 'resukisu'
        type: choice
        options:
          - resukisu
          - kernelsu-next
      clang_version:
        description: 'Pilih versi Clang'
        required: true
        default: '20'
        type: choice
        options:
          - '19'
          - '20'

jobs:
  build-kernel:
    name: Build Kernel (Clang ${{ github.event.inputs.clang_version }})
    runs-on: ubuntu-22.04

    steps:
      - name: 1. Setup Environment & Clang
        run: |
          CLANG_VER="${{ github.event.inputs.clang_version }}"
          echo "=== SETUP ENVIRONMENT & CLANG ${CLANG_VER} ==="
          sudo apt-get update
          sudo apt-get install -y build-essential bc gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi libssl-dev libfl-dev curl git zip unzip perl jq wget lsb-release software-properties-common gpg libelf-dev

          wget https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh ${CLANG_VER} all

          LLVM_BIN_PATH="/usr/lib/llvm-${CLANG_VER}/bin"
          sudo ln -sf "$LLVM_BIN_PATH/clang" /usr/bin/clang
          sudo ln -sf "$LLVM_BIN_PATH/ld.lld" /usr/bin/ld.lld
          sudo ln -sf "$LLVM_BIN_PATH/llvm-objcopy" /usr/bin/llvm-objcopy
          sudo ln -sf "$LLVM_BIN_PATH/llvm-ar" /usr/bin/llvm-ar
          sudo ln -sf "$LLVM_BIN_PATH/llvm-strip" /usr/bin/llvm-strip
          sudo ln -sf "$LLVM_BIN_PATH/llvm-nm" /usr/bin/llvm-nm
          sudo ln -sf "$LLVM_BIN_PATH/llvm-objdump" /usr/bin/llvm-objdump
          sudo ln -sf "$LLVM_BIN_PATH/llvm-readelf" /usr/bin/llvm-readelf

          echo "$LLVM_BIN_PATH" >> $GITHUB_PATH
          echo "=== VERIFIKASI COMPILER ==="
          clang --version

      - name: 2. Clone Kernel Source (millennium-base)
        run: |
          echo "=== CLONE KERNEL SOURCE ==="
          git clone --depth=1 -b millennium-base https://github.com/MillenniumOSS/android_kernel_common_android12-5.10.git kernel-source
          cd kernel-source
          sed -i 's/^SUBLEVEL =.*/SUBLEVEL = 258/' Makefile
          sed -i 's/^EXTRAVERSION =.*/EXTRAVERSION = -Panxcz-gaming/' Makefile

      - name: 3. Integrasi KernelSU (varian pilihan)
        run: |
          echo "=== INTEGRASI KERNELSU VARIAN: ${{ github.event.inputs.variant }} ==="
          cd kernel-source
          if [ "${{ github.event.inputs.variant }}" = "resukisu" ]; then
            curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s main
          else
            if curl -fsSL "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/main/kernel/setup.sh" -o /tmp/setup.sh; then
              bash /tmp/setup.sh main
            else
              echo "Setup.sh KernelSU-Next tidak ditemukan, clone manual..."
              rm -rf KernelSU
              git clone --depth=1 https://github.com/KernelSU-Next/KernelSU-Next.git KernelSU
              cp -r KernelSU/* . 2>/dev/null || true
              find KernelSU -name "*.patch" -exec patch -p1 < {} \; 2>/dev/null || true
              echo 'source "KernelSU/Kconfig"' >> Kconfig
              echo 'obj-y += KernelSU/' >> Makefile
            fi
          fi

      - name: 4. Integrasi SUSFS v2.2.0 (tanpa mirror)
        run: |
          echo "=== INTEGRASI SUSFS v2.2.0 ==="
          cd $GITHUB_WORKSPACE

          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android12-5.10 susfs-source

          HEADER_DEST="$GITHUB_WORKSPACE/kernel-source/KernelSU/kernel/ksu_susfs.h"
          mkdir -p "$(dirname "$HEADER_DEST")"

          SUSFS_HEADER=$(find susfs-source -name "ksu_susfs.h" | head -n 1)
          if [ -n "$SUSFS_HEADER" ]; then
            cp "$SUSFS_HEADER" "$HEADER_DEST"
            echo "ksu_susfs.h berhasil disalin dari susfs-source!"
          else
            echo "ksu_susfs.h tidak ditemukan, buat file minimal..."
            cat > "$HEADER_DEST" << 'EOF'
          #ifndef _KSU_SUSFS_H
          #define _KSU_SUSFS_H
          /* dummy header for SUSFS */
          #endif
          EOF
          fi

          echo "Menyalin kernel patches..."
          cp -r susfs-source/kernel_patches/fs/* kernel-source/fs/ 2>/dev/null || true
          cp -r susfs-source/kernel_patches/include/linux/* kernel-source/include/linux/ 2>/dev/null || true
          find susfs-source/kernel_patches -name "*.patch" -exec cp {} kernel-source/ \;

          cd kernel-source
          SUBLEVEL=$(grep '^SUBLEVEL = ' Makefile | awk '{print $3}')
          echo "Sublevel: 5.10.$SUBLEVEL"

          [ "$SUBLEVEL" -le "43" ] && perl -i -pe 's/(int|size_t)\s+this_len\s*=\s*min_t\s*\(\s*\1\s*,/size_t this_len = min_t(size_t,/;' fs/proc/base.c
          [ "$SUBLEVEL" -le "117" ] && {
            sed -i '/^[[:space:]]*\/\*$/,/^[[:space:]]*u32 mask = mark->mask & IN_ALL_EVENTS;$/d' fs/notify/fdinfo.c
            perl -i -pe 's/\bmask,\s*mark->ignored_mask/inotify_mark_user_mask(mark)/g' fs/notify/fdinfo.c
            perl -i -pe 's/ignored_mask:%x/ignored_mask:0/g' fs/notify/fdinfo.c
          }

          PATCH_FILE=$(ls 50_*.patch 2>/dev/null | head -n 1)
          [ -z "$PATCH_FILE" ] && { echo "Tidak ada patch SUSFS!"; exit 1; }
          echo "Menggunakan patch: $PATCH_FILE"
          patch -p1 < "$PATCH_FILE" || { echo "Patch gagal!"; find . -name "*.rej" -exec cat {} \;; exit 1; }

          [ "$SUBLEVEL" -le "209" ] && sed -i -e 's/goto show_pad;/return 0;/' ./fs/proc/task_mmu.c

      - name: 5. Konfigurasi Optimasi & Matikan BPF/BTF Total
        run: |
          echo "=== KONFIGURASI OPTIMASI & MATIKAN BPF/BTF ==="
          cd $GITHUB_WORKSPACE/kernel-source

          find . -type f \( -name "Makefile" -o -name "Kbuild" \) -exec sed -i \
            -e 's/\-Werror\b//g' -e 's/\-Werror=[^ ]*//g' \
            -e 's/\-Wstrict-prototypes//g' -e 's/\-Wimplicit-function-declaration//g' \
            -e 's/\-Wimplicit-int//g' -e 's/\-Wreturn-type//g' -e 's/\-Wdate-time//g' \
            -e 's/=strict-prototypes//g' -e 's/=implicit-function-declaration//g' \
            -e 's/=implicit-int//g' -e 's/=return-type//g' -e 's/=date-time//g' \
            -e 's/=unknown-warning-option//g' -e 's/\-implicit-function-declaration//g' \
            -e 's/\-implicit-int//g' -e 's/\-strict-prototypes//g' -e 's/\-return-type//g' \
            -e 's/\-date-time//g' {} \;

          sed -i 's/\-implicit-function-declaration//g' arch/arm64/kernel/vdso32/Makefile 2>/dev/null || true

          echo 'KBUILD_CFLAGS += -Wno-error -Wno-unknown-warning-option -Wno-pointer-sign' >> Makefile
          echo 'HOSTCFLAGS += -Wno-error -Wno-unknown-warning-option -Wno-pointer-sign' >> Makefile
          echo 'KBUILD_CFLAGS += -O3 -march=armv8.2-a+crypto' >> Makefile

          rm -rf tools/bpf
          find . -name "Makefile" -exec sed -i '/resolve_btfids/d' {} \;
          find . -name "Makefile" -exec sed -i '/prepare-resolve_btfids/d' {} \;
          echo -e "\nprepare-resolve_btfids: ;" >> Makefile
          echo -e "\nresolve_btfids: ;" >> Makefile
          echo -e "\nprepare-resolve_btfids: ;" >> arch/arm64/Makefile 2>/dev/null || true
          echo -e "\nresolve_btfids: ;" >> arch/arm64/Makefile 2>/dev/null || true

          make O=out ARCH=arm64 gki_defconfig

          ./scripts/config --file out/.config \
            --disable CONFIG_BPF \
            --disable CONFIG_BPF_SYSCALL \
            --disable CONFIG_DEBUG_INFO_BTF \
            --disable CONFIG_DEBUG_INFO_BTF_MODULES \
            --disable CONFIG_BPF_EVENTS \
            --disable CONFIG_HAVE_BPF_JIT \
            --disable CONFIG_KVM \
            --disable CONFIG_ARM64_KVM \
            --disable CONFIG_VIRTUALIZATION \
            --enable CONFIG_MODULES \
            --disable CONFIG_HEADERS_INSTALL \
            --disable CONFIG_DEBUG_INFO \
            --disable CONFIG_KASAN \
            --disable CONFIG_UBSAN \
            --enable CONFIG_LTO_NONE \
            --set-val CONFIG_HZ 1000 \
            --enable CONFIG_HZ_1000 \
            --enable CONFIG_SCHED_AUTOGROUP \
            --enable CONFIG_CFS_BANDWIDTH \
            --enable CONFIG_ENERGY_MODEL \
            --enable CONFIG_CPU_FREQ_GOV_SCHEDUTIL \
            --enable CONFIG_CPU_FREQ_GOV_PERFORMANCE \
            --enable CONFIG_CPU_FREQ_DEFAULT_GOV_SCHEDUTIL \
            --enable CONFIG_ZRAM --set-val CONFIG_ZRAM_DEF_COMP lz4 \
            --enable CONFIG_ZRAM_DEF_COMP_LZ4 \
            --enable CONFIG_TRANSPARENT_HUGEPAGE \
            --enable CONFIG_TRANSPARENT_HUGEPAGE_ALWAYS \
            --enable CONFIG_CLEANCACHE \
            --enable CONFIG_FRONTSWAP \
            --enable CONFIG_COMPACTION \
            --enable CONFIG_MIGRATION \
            --enable CONFIG_KSM \
            --enable CONFIG_LZ4_COMPRESS \
            --enable CONFIG_LZ4_DECOMPRESS \
            --enable CONFIG_CRYPTO_LZ4 \
            --enable CONFIG_CRYPTO_LZ4HC \
            --disable CONFIG_PANIC_ON_OOPS \
            --disable CONFIG_SCHED_DEBUG \
            --disable CONFIG_WERROR \
            --enable CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE \
            --set-val CONFIG_LOCALVERSION "-Panxcz-susfs"

          ./scripts/config --file out/.config \
            --enable CONFIG_KSU_SUSFS \
            --enable CONFIG_KSU_SUSFS_SUS_PATH \
            --enable CONFIG_KSU_SUSFS_SUS_MOUNT \
            --enable CONFIG_KSU_SUSFS_SUS_KSTAT \
            --enable CONFIG_KSU_SUSFS_SPOOF_UNAME \
            --disable CONFIG_KSU_SUSFS_ENABLE_LOG \
            --enable CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS \
            --enable CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG \
            --enable CONFIG_KSU_SUSFS_OPEN_REDIRECT \
            --enable CONFIG_KSU_SUSFS_SUS_MAP

          make O=out ARCH=arm64 olddefconfig

          sed -i 's/CONFIG_DEBUG_INFO_BTF=y/CONFIG_DEBUG_INFO_BTF=n/g' out/.config
          sed -i 's/CONFIG_BPF=y/CONFIG_BPF=n/g' out/.config
          sed -i 's/CONFIG_BPF_SYSCALL=y/CONFIG_BPF_SYSCALL=n/g' out/.config
          make O=out ARCH=arm64 olddefconfig
          sed -i 's/CONFIG_DEBUG_INFO_BTF=y/CONFIG_DEBUG_INFO_BTF=n/g' out/.config
          sed -i 's/CONFIG_BPF=y/CONFIG_BPF=n/g' out/.config
          sed -i 's/CONFIG_BPF_SYSCALL=y/CONFIG_BPF_SYSCALL=n/g' out/.config
          make O=out ARCH=arm64 olddefconfig

      - name: 6. Build Kernel (Image + vmlinux) dengan SKIP_BTF
        run: |
          echo "=== MULAI BUILD ==="
          cd $GITHUB_WORKSPACE/kernel-source
          CLANG_VER="${{ github.event.inputs.clang_version }}"
          export PATH="/usr/lib/llvm-${CLANG_VER}/bin:$PATH"
          export SKIP_BTF=1

          rm -rf out
          make O=out ARCH=arm64 olddefconfig

          make -j$(nproc --all) O=out ARCH=arm64 \
            HOSTCC=gcc HOSTCXX=g++ \
            CC=clang LD=ld.lld AR=llvm-ar NM=llvm-nm OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip \
            CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            LLVM=1 LLVM_IAS=1 \
            SKIP_BTF=1 \
            KCFLAGS="-Wno-error -Wno-unknown-warning-option -Wno-pointer-sign" \
            Image vmlinux 2>&1 | tee build.log

          if [ ! -f out/arch/arm64/boot/Image ]; then
            echo "=== BUILD GAGAL ==="
            tail -n 200 build.log
            exit 1
          fi
          echo "=== BUILD SUKSES ==="

      - name: 7. Simpan build.log
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ github.event.inputs.variant }}-clang${{ github.event.inputs.clang_version }}
          path: kernel-source/build.log
        if: always()

      - name: 8. Packing AnyKernel3
        run: |
          echo "=== PACKING ZIP ==="
          cd $GITHUB_WORKSPACE
          git clone --depth=1 https://github.com/OSM0SIS/AnyKernel3.git AK3
          cp kernel-source/out/arch/arm64/boot/Image AK3/
          cd AK3
          rm -rf .git*
          sed -i 's|block=.*|block=/dev/block/by-name/boot;|g' anykernel.sh
          sed -i 's|is_slot_device=.*|is_slot_device=auto;|g' anykernel.sh
          echo 'partition_list="boot_a boot_b boot dtbo_a dtbo_b";' >> anykernel.sh
          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' anykernel.sh
          zip -r9 ../Panxcz_kernel_susfs_${{ github.event.inputs.variant }}_clang${{ github.event.inputs.clang_version }}.zip *

      - name: 9. Upload ZIP
        uses: actions/upload-artifact@v4
        with:
          name: Panxcz_kernel_susfs_${{ github.event.inputs.variant }}_clang${{ github.event.inputs.clang_version }}
          path: Panxcz_kernel_susfs_${{ github.event.inputs.variant }}_clang${{ github.event.inputs.clang_version }}.zip
