name: Build Kernel GKI - Panxcz Gaming SUSFS

on:
  workflow_dispatch:

jobs:
  build-kernel:
    name: Kompilasi Kernel GKI (Android 12 - 5.10)
    runs-on: ubuntu-22.04
    
    steps:
    - name: 1. Persiapan Server & Pemasangan Clang 19
      run: |
        echo "=== MEMULAI SETUP ENVIRONMENT ==="
        sudo apt-get update
        sudo apt-get install -y build-essential bc gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi libssl-dev libfl-dev curl git zip unzip perl jq wget lsb-release software-properties-common gpg
        
        # Mengunduh dan memasang LLVM/Clang 19 resmi
        wget https://apt.llvm.org/llvm.sh
        chmod +x llvm.sh
        sudo ./llvm.sh 19 all
        
        # Membuat tautan simbolis (symlink) yang kokoh untuk seluruh toolchain LLVM 19
        LLVM_BIN_PATH="/usr/lib/llvm-19/bin"
        sudo ln -sf "$LLVM_BIN_PATH/clang" /usr/bin/clang
        sudo ln -sf "$LLVM_BIN_PATH/ld.lld" /usr/bin/ld.lld
        sudo ln -sf "$LLVM_BIN_PATH/llvm-objcopy" /usr/bin/llvm-objcopy
        sudo ln -sf "$LLVM_BIN_PATH/llvm-ar" /usr/bin/llvm-ar
        sudo ln -sf "$LLVM_BIN_PATH/llvm-strip" /usr/bin/llvm-strip
        sudo ln -sf "$LLVM_BIN_PATH/llvm-nm" /usr/bin/llvm-nm
        sudo ln -sf "$LLVM_BIN_PATH/llvm-objdump" /usr/bin/llvm-objdump
        sudo ln -sf "$LLVM_BIN_PATH/llvm-readelf" /usr/bin/llvm-readelf
        
        # Ekspor folder LLVM ke Github Path agar dibaca sistem secara global
        echo "$LLVM_BIN_PATH" >> $GITHUB_PATH
        
        echo "=== VERIFIKASI COMPILER ==="
        clang --version

    - name: 2. Kloning Source Kernel (MillenniumOSS 5.10.257)
      run: |
        echo "=== CLONING KERNEL SOURCE ==="
        # Mengambil versi Android 12 Kernel 5.10 LTS paling mutakhir (5.10.257)
        git clone --depth=1 -b android12-5.10 https://github.com/MillenniumOSS/android_kernel_common_android12-5.10.git kernel-source

    - name: 3. Integrasi SUSFS v2.1.0 Terbaru
      run: |
        echo "=== INTEGRASI SUSFS v2.1.0 ==="
        # Mengambil repositori resmi SUSFS v2.1.0
        git clone https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android12-5.10 susfs-source
        
        # Salin berkas patch dan header yang dibutuhkan
        mkdir -p kernel-source/KernelSU/kernel
        cp susfs-source/KernelSU/kernel/ksu_susfs.h kernel-source/KernelSU/kernel/ || echo "ksu_susfs.h sudah terintegrasi"
        cp -r susfs-source/kernel_patches/fs/* kernel-source/fs/
        cp -r susfs-source/kernel_patches/include/linux/* kernel-source/include/linux/
        cp susfs-source/kernel_patches/50_add_susfs_in_gki-android12-5.10.patch kernel-source/
        
        cd kernel-source
        SUBLEVEL=$(grep '^SUBLEVEL = ' Makefile | awk '{print $3}')
        echo "Sublevel kernel terdeteksi: 5.10.$SUBLEVEL"

        # [Fake Patches] Memperbaiki konflik patch bawaan untuk sub-level tertentu
        if [ "$SUBLEVEL" -le "43" ]; then
          perl -i -pe 's/(int|size_t)\s+this_len\s*=\s*min_t\s*\(\s*\1\s*,/size_t this_len = min_t(size_t,/;' fs/proc/base.c
        fi
        if [ "$SUBLEVEL" -le "117" ]; then
          sed -i '/^[[:space:]]*\/\*$/,/^[[:space:]]*u32 mask = mark->mask & IN_ALL_EVENTS;$/d' fs/notify/fdinfo.c
          perl -i -pe 's/\bmask,\s*mark->ignored_mask/inotify_mark_user_mask(mark)/g' fs/notify/fdinfo.c
          perl -i -pe 's/ignored_mask:%x/ignored_mask:0/g' fs/notify/fdinfo.c
        fi

        # Eksekusi patch utama SUSFS
        patch -p1 < 50_add_susfs_in_gki-android12-5.10.patch

        # Perbaikan bug show_pad untuk kestabilan pembacaan memori
        if [ "$SUBLEVEL" -le "209" ]; then
          sed -i -e 's/goto show_pad;/return 0;/' ./fs/proc/task_mmu.c
        fi

    - name: 4. Suntik ReSukiSU Core
      run: |
        echo "=== MENYUNTIKKAN RESUKISU CORE ==="
        cd kernel-source
        # Menjalankan script installer ReSukiSU
        curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s main
        
        # [FIX HARD KERNELSU STRUCTURE]
        # Salin secara fisik isi driver KernelSU ke folder drivers/kernelsu agar terdeteksi secara absolut
        echo "Melakukan penyelarasan fisik folder drivers/kernelsu..."
        rm -rf drivers/kernelsu
        mkdir -p drivers/kernelsu
        
        if [ -d "KernelSU/drivers" ]; then
          cp -r KernelSU/drivers/* drivers/kernelsu/
        elif [ -d "KernelSU" ]; then
          # Jika strukturnya langsung di root KernelSU
          cp -r KernelSU/* drivers/kernelsu/ || true
        fi
        
        # Pastikan file Kconfig di drivers/kernelsu tidak kosong dan merujuk ke dirinya sendiri secara lokal
        if [ -f "drivers/kernelsu/Kconfig" ]; then
          sed -i 's|source "KernelSU/drivers/Kconfig"||g' drivers/kernelsu/Kconfig || true
          # Bersihkan referensi rekursif yang bisa bikin loop
          sed -i 's|KernelSU/drivers/||g' drivers/kernelsu/Kconfig || true
          sed -i 's|KernelSU/drivers/||g' drivers/kernelsu/Makefile || true
        fi

    - name: 5. Konfigurasi Optimasi "Geyming Abis"
      run: |
        echo "=== SUNTIK OPTIMASI GAME & PERFORMA ==="
        cd kernel-source
        
        # Driver-level -Werror stripping
        find . -name "Makefile" -o -name "Kbuild" | xargs sed -i 's/-Werror//g' || true
        
        # Mengubah batas maksimum stack frame agar ramah optimasi inlining Clang 19
        sed -i 's/CONFIG_FRAME_WARN=2048/CONFIG_FRAME_WARN=4096/g' arch/arm64/configs/gki_defconfig || true
        
        # Konfigurasi tuning performa murni untuk gaming (ZRAM LZ4, hapus overhead debug log)
        {
          echo "# CONFIG_DEBUG_INFO is not set"
          echo "CONFIG_DEBUG_INFO_REDUCED=n"
          echo "CONFIG_SLUB_DEBUG=n"
          echo "CONFIG_PANIC_ON_OOPS=n"
          echo "CONFIG_LOCKUP_DETECTOR=n"
          echo "CONFIG_SCHED_DEBUG=n"
          echo "CONFIG_PROFILING=y"
          echo "CONFIG_PERF_EVENTS=y"
          echo "CONFIG_HW_PERF_EVENTS=y"
          echo "CONFIG_ARM_PMU=y"
          echo "CONFIG_FRAME_WARN=4096"
          echo "CONFIG_WERROR=n"
          echo "CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y"
          echo "CONFIG_CRYPTO_LZ4=y"
          echo "CONFIG_CRYPTO_LZ4HC=y"
          echo "CONFIG_ZRAM_DEF_COMP_LZ4=y"
        } >> arch/arm64/configs/gki_defconfig

    - name: 6. Proses Kompilasi Kernel (Clang 19)
      run: |
        echo "=== MEMULAI KOMPILASI KERNEL ==="
        cd kernel-source
        export PATH="/usr/lib/llvm-19/bin:$PATH"
        
        # Menyisipkan konfigurasi nama lokal kernel dan modul SUSFS
        {
          echo "CONFIG_KSU_SUSFS=y"
          echo "CONFIG_KSU_SUSFS_SUS_PATH=y"
          echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y"
          echo "CONFIG_KSU_SUSFS_SUS_KSTAT=y"
          echo "CONFIG_KSU_SUSFS_SPOOF_UNAME=y"
          echo "CONFIG_KSU_SUSFS_ENABLE_LOG=n"
          echo "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y"
          echo "CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y"
          echo "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y"
          echo "CONFIG_KSU_SUSFS_SUS_MAP=y"
          echo 'CONFIG_LOCALVERSION="-Panxcz-susfs"'
        } >> arch/arm64/configs/gki_defconfig
        
        # Membangun konfigurasi defconfig tanpa headers_install untuk menghindari hang/stuck
        make O=out ARCH=arm64 gki_defconfig
        make O=out ARCH=arm64 olddefconfig
        
        # Gas kompilasi dengan performa thread CPU maksimal
        make -j$(nproc --all) O=out ARCH=arm64 \
          HOSTCC=gcc HOSTCXX=g++ \
          CC=clang LD=ld.lld AR=llvm-ar NM=llvm-nm OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip \
          CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
          LLVM=1 LLVM_IAS=1

    - name: 7. Proses Packing AnyKernel3 (Hard Fix Slot A/B)
      run: |
        echo "=== PACKING ZIP INSTALASI ==="
        git clone --depth=1 https://github.com/OSM0SIS/AnyKernel3.git AK3
        
        # Memastikan file Image hasil kompilasi ada sebelum dipacking
        if [ -f kernel-source/out/arch/arm64/boot/Image ]; then
          cp kernel-source/out/arch/arm64/boot/Image AK3/
          echo "Berkas Image kernel berhasil disalin ke AnyKernel3!"
        else
          echo "Error: Berkas Image tidak ditemukan! Kompilasi gagal." && exit 1
        fi
        
        cd AK3
        # [HARD FIX PARTITION] Mengatur penargetan langsung ke partisi boot dinamis slot A/B
        sed -i 's|block=.*|block=/dev/block/by-name/boot;|g' anykernel.sh
        sed -i 's|is_slot_device=.*|is_slot_device=auto;|g' anykernel.sh
        
        # Menambahkan daftar partisi target eksplisit agar instalasi tidak salah kamar
        echo 'partition_list="boot_a boot_b boot dtbo_a dtbo_b";' >> anykernel.sh
        
        # Matikan validasi perangkat agar paket ZIP bersifat universal pada sistem GKI
        sed -i 's/do.devicecheck=1/do.devicecheck=0/g' anykernel.sh
        
        # Mengemas menjadi berkas ZIP siap flash
        zip -r9 ../Panxcz_kernel_susfs.zip *

    - name: 8. Mengunggah Hasil Build Jadi
      uses: actions/upload-artifact@v4
      with:
        name: Panxcz_kernel_susfs
        path: Panxcz_kernel_susfs.zip
